# This file configures the BACKEND Railway service.
# The frontend service ignores this (it has its own config in Railway dashboard).

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
